<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"
    />
    <title>초록</title>
    <link rel="stylesheet" href="/styles/main.css" />
    <link
      rel="stylesheet"
      href="https://uicdn.toast.com/editor/latest/toastui-editor.css"
    />
  </head>

  <body class="grey-bg">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <%- include('nav.ejs')%>
    <form class="form-box">
      <h4 style="margin-bottom: 10px; margin-top: 0">글 작성</h4>
      <p class="write-category-header">카테고리</p>
      <select class="write-category-select">
        <% for (let i=0; i < category.length; i++){%>
            <option value=<%=category[i].name%> <%= category[i].name === selectedCategory && 'selected' %> ><%=category[i].name%></option>
          <%}%>
      </select>
      <input
        value="<%=post.title%>"
        class="write-title"
        name="title"
        type="text"
        placeholder="제목을 입력해주세요"
      />
      <!-- <textarea
        class="write-content"
        name="content"
        type="text"
        placeholder="내용을 입력해주세요"
      >
<%=post.content%></textarea
      > -->
      <div id="editor"></div>
      <button class="write-submit-btn" id="submit-btn">전송</button>
    <input type="hidden" id="initial-content" value="<%- post.content.replace(/"/g, '&quot;') %>">
    </form>
    
    
    <script src="https://uicdn.toast.com/editor/latest/toastui-editor-all.min.js"></script>
    <!-- 한국어 패치 -->
    <script src="https://uicdn.toast.com/editor/latest/i18n/ko-kr.min.js"></script>
    <script>
            const content = document.querySelector('#initial-content').value;

      const getToolbarItems = () => {
        if (window.innerWidth < 768) {
          return [["bold", "italic"], ["image"], ["link"]];
        } else {
          return [
            ["heading", "bold", "italic", "strike"],
            ["hr", "quote"],
            ["image", "link"],
            ["code", "codeblock"],
          ];
        }
      };

      const Editor = toastui.Editor;
      const editor = new Editor({
        el: document.querySelector("#editor"),
        height: "600px",
        initialEditType: "markdown",
        initialValue: content,
        toolbarItems: getToolbarItems(),
        previewStyle: "tab",
        placeholder: "내용을 입력해주세요",
        language: "ko-KR",
        hooks: {
          async addImageBlobHook(blob, callback) {
            console.log("이미지 업로드 됨");
            try {
              const formData = new FormData();
              formData.append("img1", blob);

              const response = await axios.post("/upload-image", formData);
              console.log(response);
              callback(response.data.url, "image");
            } catch (error) {
              if (error.response) {
                alert(error.response.data.message);
              }
            }
          },
        },
      });
    </script>
  <!-- 글 수정 요청 -->
    <script>
        document.querySelector(".form-box").addEventListener("submit", (e) => {
            e.preventDefault();
      });

      document
        .querySelector("#submit-btn")
        .addEventListener("click", async () => {
          const title = document.querySelector(".write-title").value;
          const content = editor.getMarkdown();
          const category = document.querySelector(".write-category-select").value;
          const formData = new FormData();
          formData.append("title", title);
          formData.append("content", content);
          formData.append("_id", "<%=post._id%>");
          formData.append("category", category);
          try {
            const { data } = await axios.patch('/edit', formData)
            alert(data.message);
            window.location.replace("/detail/<%=post._id%>");
          } catch (error) {
            if (error.response){
                alert(error.response.data.message);
            }
          }
        })
    </script>
  </body>
</html>
